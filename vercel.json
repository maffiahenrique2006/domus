{
  "buildCommand": "export PORT=5000 BASE_PATH=/ NODE_ENV=production; pnpm --filter @workspace/domus run build",
  "outputDirectory": "artifacts/domus/dist/public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ]
}
